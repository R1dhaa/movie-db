@page "{id:int?}"
@model RazorPagesMovie.Pages_Movies.IndexModel

@{
    ViewData["Title"] = "Movie List";
}

<style>
    body {
        background-image: url('/images/bg.png');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
    }

    .full-screen-center {
        min-height: calc(100vh - 140px);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .card {
        background-color: rgba(0, 0, 0, 0.5);
        border: none;
        color: white;
    }

    .card-header {
        background-color: rgba(0, 0, 0, 0.6);
    }

    .btn-transparent {
        background-color: rgba(255, 255, 255, 0.2);
        color: white;
        border: 1px solid white;
    }

    .btn-transparent:hover {
        background-color: rgba(255, 255, 255, 0.4);
        color: black;
    }
</style>

<div class="container full-screen-center">
    <div class="col-md-10">
        <div class="card shadow bg-dark bg-opacity-75 text-white">
            <div class="card-header text-center border-0">
                <h4 class="mb-0">Movie List</h4>
            </div>
            <div class="card-body">

                @if (User.IsInRole("Admin"))
                {
                    <div class="mb-3 text-end">
                        <a class="btn btn-primary" asp-page="Create">Create New</a>
                    </div>
                }

                <form method="get" class="row g-3 align-items-end mb-4">
                    <div class="col-md-3">
                        <label asp-for="MovieGenre" class="form-label">Genre</label>
                        <select asp-for="MovieGenre" asp-items="Model.Genres" class="form-select">
                            <option value="">All</option>
                        </select>
                    </div>

                    <div class="col-md-4">
                        <label asp-for="SearchString" class="form-label">Title</label>
                        <input asp-for="SearchString" class="form-control" placeholder="Search by title" />
                    </div>

                    <div class="col-md-auto d-flex gap-2">
                        <button type="submit" class="btn btn-outline-light">Filter</button>

                        <a class="btn btn-success"
                            asp-page="Export"
                            asp-route-SearchString="@Model.SearchString"
                            asp-route-MovieGenre="@Model.MovieGenre"
                            asp-route-SortField="@Model.SortField"
                            asp-route-SortOrder="@Model.SortOrder"
                            asp-route-exportMode="all">
                            Export All
                            </a>

                            <a class="btn btn-warning"
                            asp-page="Export"
                            asp-route-SearchString="@Model.SearchString"
                            asp-route-MovieGenre="@Model.MovieGenre"
                            asp-route-PageIndex="@Model.PageIndex"
                            asp-route-SortField="@Model.SortField"
                            asp-route-SortOrder="@Model.SortOrder"
                            asp-route-exportMode="page">
                            Export Current Page
                            </a>

                    </div>
                </form>

                <div class="table-responsive">
                    <table class="table table-bordered table-hover table-dark">
                        <thead>
                            <tr>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-SortField="Title"
                                       asp-route-SortOrder="@(Model.SortField == "Title" && Model.SortOrder == "asc" ? "desc" : "asc")"
                                       asp-route-SearchString="@Model.SearchString"
                                       asp-route-MovieGenre="@Model.MovieGenre"
                                       asp-route-PageIndex="1">
                                        Title @(Model.SortField == "Title" ? (Model.SortOrder == "asc" ? "↑" : "↓") : "")
                                    </a>
                                </th>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-SortField="ReleaseDate"
                                       asp-route-SortOrder="@(Model.SortField == "ReleaseDate" && Model.SortOrder == "asc" ? "desc" : "asc")"
                                       asp-route-SearchString="@Model.SearchString"
                                       asp-route-MovieGenre="@Model.MovieGenre"
                                       asp-route-PageIndex="1">
                                        Release Date @(Model.SortField == "ReleaseDate" ? (Model.SortOrder == "asc" ? "↑" : "↓") : "")
                                    </a>
                                </th>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-SortField="Genre"
                                       asp-route-SortOrder="@(Model.SortField == "Genre" && Model.SortOrder == "asc" ? "desc" : "asc")"
                                       asp-route-SearchString="@Model.SearchString"
                                       asp-route-MovieGenre="@Model.MovieGenre"
                                       asp-route-PageIndex="1">
                                        Genre @(Model.SortField == "Genre" ? (Model.SortOrder == "asc" ? "↑" : "↓") : "")
                                    </a>
                                </th>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-SortField="Price"
                                       asp-route-SortOrder="@(Model.SortField == "Price" && Model.SortOrder == "asc" ? "desc" : "asc")"
                                       asp-route-SearchString="@Model.SearchString"
                                       asp-route-MovieGenre="@Model.MovieGenre"
                                       asp-route-PageIndex="1">
                                        Price @(Model.SortField == "Price" ? (Model.SortOrder == "asc" ? "↑" : "↓") : "")
                                    </a>
                                </th>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-SortField="Rating"
                                       asp-route-SortOrder="@(Model.SortField == "Rating" && Model.SortOrder == "asc" ? "desc" : "asc")"
                                       asp-route-SearchString="@Model.SearchString"
                                       asp-route-MovieGenre="@Model.MovieGenre"
                                       asp-route-PageIndex="1">
                                        Rating @(Model.SortField == "Rating" ? (Model.SortOrder == "asc" ? "↑" : "↓") : "")
                                    </a>
                                </th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Movie)
                            {
                                <tr>
                                    <td>@item.Title</td>
                                    <td>@item.ReleaseDate.ToString("dd-MM-yyyy")</td>
                                    <td>@item.Genre</td>
                                    <td>₹ @item.Price.ToString("0.00")</td>
                                    <td>@item.Rating</td>
                                    <td>
                                        <div class="d-flex flex-row justify-content-start align-items-center gap-2 flex-wrap">
                                            @if (User.IsInRole("Admin"))
                                            {
                                                <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-sm btn-transparent me-1">Edit</a>
                                                <a asp-page="./Details" asp-route-id="@item.Id" class="btn btn-sm btn-transparent me-1">Details</a>
                                                <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-sm btn-transparent">Delete</a>
                                            }
                                            else
                                            {
                                                <a asp-page="./Details" asp-route-id="@item.Id" class="btn btn-sm btn-transparent">Details</a>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <nav aria-label="Page navigation" class="mt-4">
                    <ul class="pagination justify-content-center">
                        <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                            <a class="page-link"
                               asp-page="./Index"
                               asp-route-PageIndex="@(Model.PageIndex - 1)"
                               asp-route-SearchString="@Model.SearchString"
                               asp-route-MovieGenre="@Model.MovieGenre"
                               asp-route-SortField="@Model.SortField"
                               asp-route-SortOrder="@Model.SortOrder">
                                Previous
                            </a>
                        </li>

                        @{
                            var maxDisplayPages = 5;
                            var startPage = Math.Max(1, Model.PageIndex - maxDisplayPages / 2);
                            var endPage = Math.Min(Model.TotalPages, startPage + maxDisplayPages - 1);
                            if ((endPage - startPage) < (maxDisplayPages - 1))
                            {
                                startPage = Math.Max(1, endPage - maxDisplayPages + 1);
                            }
                        }

                        @for (var i = startPage; i <= endPage; i++)
                        {
                            <li class="page-item @(i == Model.PageIndex ? "active" : "")">
                                <a class="page-link"
                                   asp-page="./Index"
                                   asp-route-PageIndex="@i"
                                   asp-route-SearchString="@Model.SearchString"
                                   asp-route-MovieGenre="@Model.MovieGenre"
                                   asp-route-SortField="@Model.SortField"
                                   asp-route-SortOrder="@Model.SortOrder">
                                    @i
                                </a>
                            </li>
                        }

                        <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                            <a class="page-link"
                               asp-page="./Index"
                               asp-route-PageIndex="@(Model.PageIndex + 1)"
                               asp-route-SearchString="@Model.SearchString"
                               asp-route-MovieGenre="@Model.MovieGenre"
                               asp-route-SortField="@Model.SortField"
                               asp-route-SortOrder="@Model.SortOrder">
                                Next
                            </a>
                        </li>
                    </ul>
                </nav>

            </div>
        </div>
    </div>
</div>
